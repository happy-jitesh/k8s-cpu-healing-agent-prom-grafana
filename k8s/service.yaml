apiVersion: v1
kind: Service

metadata:
  name: cpu-demo-app
  namespace: prod
  labels:
    app: cpu-demo-app

spec:
  selector:
    app: cpu-demo-app

  ports:
  - name: http
    port: 80
    targetPort: 80